<p>
  If you save Record without artist id then give an error 
  <b>ActiveRecord::RecordInvalid (Validation failed: Artist must exist)</b>
</p>

<div class="row">
  <div class="mx-auto col-10 col-md-8 col-lg-6">
    <%= form_with model: @song do |f| %>
      <div class="row mb-4">
        <div class="col">
          <div class="form-outline">
            <%= f.label :Song_name, class: "form-label" %>
            <%= f.text_field :name, class: "form-control" %>
          </div>
        </div>
      </div>
      <div class="row mb-4">
        <div class="col">
          <div class="form-outline">
            <%= f.label :Select_Artist, class: "form-label" %>
            <%#= f.collection_select :artist_id, @artists, :id, :name, selected: @song.artist_id,  class: "form-control" %>
            <%= f.collection_select :artist_id, @artists, :id, :name, 
                                    prompt: true, 
                                    selected: @song.artist_id %>

            </div>
        </div>
      </div>

      <%= f.submit 'Submit', class: "btn btn-primary btn-block mb-4" %>
    <% end %>
  </div>
</div>